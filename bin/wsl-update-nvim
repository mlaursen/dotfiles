#/bin/sh

version="$1"

if [ -z "$version" ]; then
  print "Usage: update-nvim 0.12.0"
  exit 1
fi

image_name=nvim-linux-x86_64.appimage

set -x

curl -LO https://github.com/neovim/neovim/releases/download/v$version/$image_name
chmod u+x $image_name
sudo mv $image_name /usr/local/bin/nvim
